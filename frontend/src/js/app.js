/**
 * Suma dos números.
 * Función pura de ejemplo para validar el pipeline de testing.
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
export function add(a, b) {
  return a + b;
}

// Lógica inicial del shell
console.log("IntraMessenger frontend inicializado.");
